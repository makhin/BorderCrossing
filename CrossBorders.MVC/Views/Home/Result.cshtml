@model System.Collections.Generic.Dictionary<System.DateTime,string>

<div class="text-center">
    <table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>Country</th>
            </tr>
        </thead>
        <tbody>
            @foreach (KeyValuePair<DateTime, string> entry in Model)
            {
                <tr>
                    <td>@entry.Key</td>
                    <td>@entry.Value</td>
                </tr>
            }
        </tbody>
    </table>
    
</div>
